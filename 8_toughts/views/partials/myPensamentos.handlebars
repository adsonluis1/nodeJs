<div class="div-myPensamentos" id="{{id}}">
    <div class="div-myTitle">
        <h3>Pensador: {{user.nome}}</h3>
        <h3>{{user.email}}</h3>
        <h2>{{title}}</h2>
    </div>

    <div class="div-controllsMypensamentos">
        <a href="/thought/delete/{{id}}" class="trash"><i class="bi bi-trash"></i></a>
        <a href="/thought/edit/{{id}}" class="edit"><i class="bi bi-pencil-square"></i></a>
    </div>

</div>